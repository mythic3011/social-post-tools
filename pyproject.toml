[project]
name = "social-post-tools"
version = "4.3.1"
description = "Python build and browser-test tooling for Social Post Tools."
requires-python = ">=3.13,<3.14"
dependencies = []

[dependency-groups]
dev = [
  "websocket-client==1.9.0",
]

[tool.uv]
package = false
required-version = ">=0.10.0,<0.13"
default-groups = ["dev"]
